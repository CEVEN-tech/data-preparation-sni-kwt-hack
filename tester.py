import pandas as pd
import numpy as np
# df = pd.DataFrame(np.arange(12).reshape(3, 4),
#                   columns=['A', 'B', 'C', 'D'])
# #print(df)
# dfWithRemoved = df.drop([0, 1], inplace=False)
# print(dfWithRemoved)

print([i for i in range(10)])